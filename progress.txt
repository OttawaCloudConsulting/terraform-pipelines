# Progress: Provider Override for Cross-Account Credential Flow
# Source: prd.md + docs/ARCHITECTURE_AND_DESIGN.md
# Spec: docs/ARCHITECTURE_AND_DESIGN.md is the authoritative design reference
# Context: docs/REFINEMENT_2.md is the problem analysis

## Previous Approach (Superseded)
# The self-assumption + backend role_arn approach from the first attempt had
# role chaining failures. Replaced with provider override approach below.

## Features

[x] Feature 1: Buildspecs — Replace STS assume-role with provider override
    - plan.yml: generate _pipeline_override.tf with assume_role block, remove STS assume-role
    - deploy.yml: generate _pipeline_override.tf with assume_role block, remove STS assume-role
    - destroy.yml: generate _pipeline_override.tf with assume_role block, remove STS assume-role
    - Remove assume_role from backend.hcl (no longer needed — CodeBuild SR instance profile handles S3)
    - post_build: rm -f _pipeline_override.tf in all three buildspecs
    - terraform validate passes for both variants and all examples
    NOTES: Completed 2026-02-16.
    All three buildspecs updated. Override file generated via cat + envsubst pattern.
    Backend config simplified (no assume_role line). Cleanup in post_build.

[x] Feature 2: IAM — Remove self-assumption
    - Remove AllowSelfAssumption trust policy statement from CodeBuild SR
    - Remove self-ARN from AssumeRole permission resource list
    - terraform validate passes for both variants and all examples
    NOTES: Completed 2026-02-16.
    Trust policy simplified to codebuild.amazonaws.com only.
    AssumeRole resource list now just dev + prod deployment roles.

[x] Feature 3: Remove STATE_ACCESS_ROLE_ARN env var
    - Remove from plan-dev, plan-prod, deploy-dev, deploy-prod in core locals.tf
    - Remove from destroy project in default-dev-destroy/main.tf
    - terraform validate passes for both variants and all examples
    NOTES: Completed 2026-02-16.
    Removed from all 4 core project env_vars and destroy project env_var block.

[x] Feature 4: Documentation
    - CLAUDE.md: Updated credential flow description to provider override pattern
    - progress.txt: Rewritten to reflect new approach
    NOTES: Completed 2026-02-16.

## Verification
- All 3 modules (core, default, default-dev-destroy) pass terraform validate
- All 9 examples pass terraform init -backend=false && terraform validate && terraform fmt -check
